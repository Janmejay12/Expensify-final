<resources>
    <!-- Base application theme -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Custom style for CalendarView date text -->
    <style name="CalendarDateTextAppearance" parent="TextAppearance.AppCompat">
        <item name="android:textColor">#000000</item> <!-- Set date text color to black -->
        <item name="android:textSize">16sp</item>      <!-- Adjust text size if needed -->
    </style>

    <!-- Custom style for CalendarView weekday names -->
    <style name="CalendarWeekDayTextAppearance" parent="TextAppearance.AppCompat"></style>>
        </resources>